Official Office UI patch for Virtual Company OS.
Unzip into the existing repository root, then git add/commit/push.
It uses the existing backend endpoints and adds automatic processing of assigned tasks.
OpenAI LLM remains dependent on OPENAI_API_KEY being securely configured on Render.
